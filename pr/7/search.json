{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Maven Documentation","text":"","path":["Maven Documentation"],"tags":[]},{"location":"authoring/","level":1,"title":"Plugins","text":"","path":["Plugins"],"tags":[]},{"location":"community/","level":1,"title":"Community","text":"","path":["Community"],"tags":[]},{"location":"get-started/","level":1,"title":"Getting Started","text":"","path":["Getting Started"],"tags":[]},{"location":"get-started/basic-project-sample/","level":1,"title":"Basic project example","text":"<p>A small step by step example project to cover the basics</p> <ul> <li>Structure of a Maven project</li> <li>executing Maven build</li> <li>Adding a dependency</li> <li>Adding a plugin</li> <li>Adding tests</li> </ul>","path":["Getting Started","Basic project example"],"tags":[]},{"location":"get-started/install-maven/","level":1,"title":"Installing Apache Maven","text":"<p>Apache Maven is a command-line build tool, so installing it means making the <code>mvn</code> command available from a terminal.</p> <p>This guide covers three steps:</p> <ol> <li>installing and configuring a Java Development Kit (JDK);</li> <li>installing Maven;</li> <li>verifying the Maven installation.</li> </ol>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#prerequisites-a-working-jdk","level":2,"title":"Prerequisites: a working JDK","text":"<p>Maven runs on the Java platform and requires a Java Development Kit (JDK) to execute. A JDK (not a JRE) is required, because Maven invokes compiler tools such as <code>javac</code> during a build.</p> <p>The minimum required JDK version depends on the Maven version in use:</p> Maven version Minimum JDK Maven 3.x JDK 8 Maven 4.x JDK 17 <p>Any modern JDK distribution is suitable.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#verify-your-jdk-installation","level":3,"title":"Verify your JDK installation","text":"<p>Before installing Maven, confirm that a compatible JDK is available on the system.</p> <p>Open a new terminal and run:</p> <pre><code>java -version\n</code></pre> <p>The output should be similar to:</p> <pre><code>openjdk version \"21.0.4\" 2024-07-16 LTS\nOpenJDK Runtime Environment (build 21.0.4+7-LTS)\nOpenJDK 64-Bit Server VM (build 21.0.4+7-LTS, mixed mode, sharing)\n</code></pre> <p>If the command is not found, install a JDK and verify the installation again.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#configure-java_home","level":3,"title":"Configure <code>JAVA_HOME</code>","text":"<p>Maven uses the <code>JAVA_HOME</code> environment variable to locate the JDK at runtime. Maven may fail to start when <code>JAVA_HOME</code> is missing or points to an invalid directory, even when the <code>java</code> executable is on the <code>PATH</code>. Configure <code>JAVA_HOME</code> so that it is available in every shell session that runs Maven.</p> macOS / Linux (bash, zsh)Windows (PowerShell) <p>Add the following lines to the shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>, or <code>~/.profile</code>), and replace the path with the actual JDK location:</p> <pre><code>export JAVA_HOME=/path/to/your/jdk\nexport PATH=\"$JAVA_HOME/bin:$PATH\"\n</code></pre> <p>On macOS, the helper <code>/usr/libexec/java_home</code> returns the currently installed JDK location:</p> <pre><code>export JAVA_HOME=$(/usr/libexec/java_home)\n</code></pre> <p>Reload the shell (<code>source ~/.zshrc</code>) or open a new terminal so the change takes effect.</p> <p>Set the variable for the current user account:</p> <pre><code>[Environment]::SetEnvironmentVariable(\"JAVA_HOME\", \"C:\\Program Files\\Java\\jdk-21\", \"User\")\n</code></pre> <p>Alternatively, configure the variable through the graphical interface. Navigate to <code>Settings</code> → <code>System</code> → <code>About</code> → <code>Advanced system settings</code> → <code>Environment Variables</code>, or search for <code>Environment Variables</code> using the Windows search bar.</p> <p>Open a new PowerShell or Command Prompt window so the change takes effect.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-maven","level":2,"title":"Install Maven","text":"<p>There are two common ways to install Maven:</p> <ul> <li>Through a package manager. This is the quickest path on most systems, and the package manager handles upgrades.</li> <li>From the official binary archive. This gives full control over the installed version and its location on disk.</li> </ul> <p>Both approaches result in a working <code>mvn</code> command. Most package managers configure the <code>PATH</code> automatically; the binary-archive method requires this to be done manually, as described in Add Maven to the <code>PATH</code>.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-with-a-package-manager","level":3,"title":"Install with a package manager","text":"<p>The package managers listed below are some of the most commonly used. They are not the only options — other Java toolchain managers can install Maven as well.</p> Cross-platformmacOSLinuxWindows <p>SDKMAN! is available on macOS, Linux, and Windows Subsystem for Linux, and can install Maven alongside several JDK distributions:</p> <pre><code>sdk install maven\n</code></pre> <p>SDKMAN! adds <code>mvn</code> to the <code>PATH</code> automatically.</p> <p>With Homebrew:</p> <pre><code>brew install maven\n</code></pre> <p>With MacPorts:</p> <pre><code>sudo port install maven3\n</code></pre> <p>The exact command depends on the distribution's package manager:</p> <pre><code># Debian / Ubuntu\nsudo apt update\nsudo apt install maven\n\n# Fedora / RHEL / CentOS Stream (dnf)\nsudo dnf install maven\n\n# Older RHEL / CentOS (yum)\nsudo yum install maven\n</code></pre> <p>Distributions other than those listed (for example, Arch Linux) ship Maven through their own package managers as well.</p> <p>With Chocolatey:</p> <pre><code>choco install maven\n</code></pre> <p>With Scoop:</p> <pre><code>scoop install main/maven\n</code></pre> <p>Package manager versions may lag behind</p> <p>Every package manager ships Maven some time after a release is published by the Apache Maven team, because each tool depends on that release as its source. For tools such as Homebrew, Chocolatey, Scoop, and SDKMAN!, the delay is usually a few days. Linux distribution repositories often lag behind by weeks or months. To install a specific (especially the latest) Maven version, prefer the binary archive or SDKMAN!.</p> <p>After the package manager finishes, continue with Verify the installation.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-from-the-binary-archive","level":3,"title":"Install from the binary archive","text":"<p>This method works on any system and installs the exact version published by the Apache Maven team.</p> <p>The steps below use Maven <code>3.9.16</code> as an example. Replace the version number with the version that was downloaded.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#1-download-the-archive","level":4,"title":"1. Download the archive","text":"<p>Open the Maven download page and download the latest binary distribution. Two formats are offered:</p> <ul> <li><code>apache-maven-&lt;version&gt;-bin.zip</code> — works on every platform and is the most convenient on Windows;</li> <li><code>apache-maven-&lt;version&gt;-bin.tar.gz</code> — convenient on macOS and Linux.</li> </ul> <p>Verify the integrity of the download (optional but recommended)</p> <p>For production setups, check the downloaded archive against the published PGP signatures and checksums using the public KEYS file. This ensures the archive has not been tampered with during transfer.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#2-extract-the-archive","level":4,"title":"2. Extract the archive","text":"<p>Extract the archive into a directory of choice, for example:</p> <ul> <li>macOS / Linux: <code>/opt/apache-maven-3.9.16</code> or <code>~/tools/apache-maven-3.9.16</code></li> <li>Windows: <code>C:\\apache-maven-3.9.16</code> or <code>C:\\Program Files\\Apache\\apache-maven-3.9.16</code></li> </ul> macOS / LinuxWindows (PowerShell) <p>The example below assumes the archive <code>apache-maven-3.9.16-bin.tar.gz</code> was downloaded, and extracts it into <code>/opt</code>, which creates <code>/opt/apache-maven-3.9.16</code>:</p> <pre><code>sudo tar -xzf apache-maven-3.9.16-bin.tar.gz -C /opt\n</code></pre> <p>The example below assumes the archive <code>apache-maven-3.9.16-bin.zip</code> was downloaded, and extracts it into <code>C:\\</code>, which creates <code>C:\\apache-maven-3.9.16</code>:</p> <pre><code>Expand-Archive -Path apache-maven-3.9.16-bin.zip -DestinationPath C:\\\n</code></pre>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#3-add-maven-to-the-path","level":4,"title":"3. Add Maven to the <code>PATH</code>","text":"<p>To make the <code>mvn</code> command available from any terminal, add the <code>bin</code> directory of the Maven installation to the <code>PATH</code> environment variable.</p> <p>Note</p> <p>If Maven was installed with a package manager (Install with a package manager), <code>PATH</code> is already configured and this step can be skipped. The binary archive does not update <code>PATH</code> — follow the steps below to add Maven manually.</p> <p>The examples below assume Maven <code>3.9.16</code> was extracted to the directory shown in the previous step.</p> macOS / Linux (bash, zsh)Windows (PowerShell) <p>Add the following line to the shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>, or <code>~/.profile</code>):</p> <pre><code>export PATH=\"/opt/apache-maven-3.9.16/bin:$PATH\"\n</code></pre> <p>Reload the shell, for example:</p> <pre><code>source ~/.zshrc\n</code></pre> <p>The snippet below appends Maven's <code>bin</code> directory to the user-level <code>Path</code>. It is safe to re-run: the directory is only added when it is not already present.</p> <pre><code>$mavenBin = \"C:\\apache-maven-3.9.16\\bin\"\n$userPath = [Environment]::GetEnvironmentVariable(\"Path\", \"User\")\nif ($userPath -notlike \"*$mavenBin*\") {\n  [Environment]::SetEnvironmentVariable(\"Path\", \"$userPath;$mavenBin\", \"User\")\n}\n</code></pre> <p>Alternatively, use the graphical interface. Navigate to <code>Settings</code> → <code>System</code> → <code>About</code> → <code>Advanced system settings</code> → <code>Environment Variables</code> (or search for <code>Environment Variables</code> using the Windows search bar), then append <code>C:\\apache-maven-3.9.16\\bin</code> to the <code>Path</code> variable.</p> <p>Open a new terminal so the change takes effect.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#verify-the-installation","level":2,"title":"Verify the installation","text":"<p>Open a new terminal so the latest environment variables are picked up, and run:</p> <pre><code>mvn -v\n</code></pre> <p>The alternative <code>mvn --version</code> produces the same output.</p> <p>The output reports the installed Maven version, the Maven installation directory, the JDK Maven is using, and basic operating-system information. Example:</p> <pre><code>Apache Maven 3.9.16 (2bdd9fddda4b155ebf8000e807eb73fd829a51d5)\nMaven home: /opt/apache-maven-3.9.16\nJava version: 21.0.4, vendor: Eclipse Adoptium, runtime: /opt/jdk-21\nDefault locale: en_US, platform encoding: UTF-8\nOS name: \"linux\", version: \"6.8.0\", arch: \"amd64\", family: \"unix\"\n</code></pre> <p>If such output is shown, Maven is correctly set up and ready to use.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#troubleshooting","level":2,"title":"Troubleshooting","text":"<code>mvn: command not found</code> (or <code>'mvn' is not recognized...</code>) <p>The <code>mvn</code> command is not on the <code>PATH</code>. Confirm that:</p> <ul> <li>the <code>bin</code> directory inside the Maven installation is on <code>PATH</code>;</li> <li>the current terminal was opened after the environment variables were updated.</li> </ul> <p>Inspect the current <code>PATH</code> with <code>echo $PATH</code> (macOS/Linux), <code>echo $env:PATH</code> (Windows PowerShell), or <code>echo %PATH%</code> (Windows Command Prompt).</p> <code>JAVA_HOME is not defined correctly</code> <p>Maven cannot find the JDK. Check that:</p> <ul> <li><code>JAVA_HOME</code> points to a directory where a JDK is installed (not a JRE, and not the <code>bin/</code> subdirectory of a JDK);</li> <li>that directory exists and contains <code>bin/java</code> (or <code>bin\\java.exe</code> on Windows).</li> </ul> <p>Inspect the current value with <code>echo $JAVA_HOME</code> (macOS/Linux), <code>echo $env:JAVA_HOME</code> (Windows PowerShell), or <code>echo %JAVA_HOME%</code> (Windows Command Prompt).</p> Wrong Java version is used <p>The output of <code>mvn -v</code> reports the JDK Maven is using. If it does not match the expected JDK, either <code>JAVA_HOME</code> or <code>PATH</code> points to a different JDK installation. Update whichever is incorrect and open a new shell.</p> <p>Each Maven version also requires a minimum JDK version, as listed in Prerequisites: a working JDK. For example, running Maven 4 with a JDK older than 17 will fail.</p> Behind a corporate proxy? <p>Maven may be unable to download dependencies on first use when running behind a corporate proxy. Configure a proxy in <code>~/.m2/settings.xml</code> as described in the Guide to Configuring Maven.</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#whats-next","level":2,"title":"What's next?","text":"<p>Once Maven is installed and verified, the next step is to build a sample project.</p> <p>Continue: Basic project sample →</p>","path":["Getting Started","Installing Apache Maven"],"tags":[]},{"location":"get-started/what-is-maven/","level":1,"title":"What is Maven (for)?","text":"<p>Apache Maven is a free, open-source build tool, primarily used to build Java applications. It is one of the most widely used build tools in the Java ecosystem and is designed to be easy to use, even in complex enterprise scenarios.</p> <p>In short: instead of writing and maintaining a custom build script for every project, the project's structure and dependencies are described in a single file (<code>pom.xml</code>), and Maven figures out how to build it.</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#what-maven-does-for-you","level":2,"title":"What Maven does for you","text":"<p>A lot of tasks have to be done to build a software project, for example compiling sources, running tests, packaging the result, and managing third-party libraries. Maven automates these tasks and performs them in a consistent, predefined way, so that they do not have to be solved again for every project.</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#1-it-builds-your-project","level":3,"title":"1. It builds your project","text":"<p>Maven compiles the code, runs the tests, and packages the result as an archive. To perform all of these steps, only one command is needed:</p> <pre><code>mvn package\n</code></pre> <p>These steps run in a fixed, predictable order called the build lifecycle. No custom build script is required, because Maven already knows the steps.</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#2-it-manages-your-dependencies","level":3,"title":"2. It manages your dependencies","text":"<p>Almost every real project uses third-party libraries. Instead of downloading the required files by hand and checking them into the repository, Maven handles this. Once the required libraries are declared in <code>pom.xml</code>, Maven will:</p> <ul> <li>download each library from a central repository (such as Maven Central),</li> <li>download any other library that library needs (its transitive dependencies),</li> <li>cache it locally so it is not re-downloaded every time it is needed,</li> <li>make it available to the compiler, tests, and packaging step.</li> <li>and automate many other parts of the build lifecycle, such as compilation, testing, packaging, reporting, and plugin execution.</li> </ul>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#3-it-gives-every-project-the-same-shape","level":3,"title":"3. It gives every project the same shape","text":"<p>Maven follows a standard directory and project layout. Following the principle of convention over configuration, this makes it easy to switch between Maven projects, because the structure is always familiar.</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#4-it-is-extended-through-plugins","level":3,"title":"4. It is extended through plugins","text":"<p>The actual work is performed by plugins. Maven ships with a rich set of plugins, covering the most needed functionality such as compiling, running tests, and packaging archives. In addition to that, the Maven community provides plugins for almost anything else that might be needed, including code quality checks, test coverage, container image builds, and deployment.</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#problems-maven-solves","level":2,"title":"Problems Maven solves","text":"<p>When working on a Java project without a proper build tool, some of these questions may sound familiar. Maven is designed to solve them:</p> <ul> <li>\"How do I build this project?\"   The build is portable and produces the same result on every machine, so the project can be built with a single <code>mvn package</code> command, the same way on every developer machine and on the CI server.</li> <li>\"Do I have all the libraries I need, and in which versions?\"   Maven downloads them, including the libraries those libraries depend on, with versions pinned in <code>pom.xml</code> and resolved the same way on every machine and CI server.</li> <li>\"Where does source code belong? Where are tests stored? Where is everything in this project?\"   Maven uses a standard directory layout, so the project structure is always familiar, even when joining a new team.</li> <li>\"How do I share my library with other projects?\"   Maven can publish the build output to a repository such as Maven Central, which makes it easy to share libraries with the wider Java community.</li> </ul>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#when-maven-is-a-good-fit","level":2,"title":"When Maven is a good fit","text":"<p>Maven is a good choice when the following are needed:</p> <ul> <li>a portable build that behaves the same on every machine, including CI servers,</li> <li>automatic dependency resolution from public or private repositories,</li> <li>a conventional project layout that other tools (IDEs, CI systems, code-quality tools) already understand,</li> <li>a large ecosystem of ready-made plugins,</li> <li>support for projects of any size, from a single library to a large enterprise application.</li> </ul>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#what-maven-is-not","level":2,"title":"What Maven is not","text":"<p>To avoid surprises later, it helps to know what Maven is not:</p> <ul> <li>It is not an IDE. All modern IDEs integrate Maven out of the box.</li> <li>It is not a CI server. Maven runs the build; the CI server runs Maven. Maven is also not a deployment tool: in the past, it was sometimes misused to distribute build artifacts to production, which is not its purpose.</li> </ul>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"get-started/what-is-maven/#next-steps","level":2,"title":"Next steps","text":"<p>With a clear picture of Maven's role and purpose, the next step is to install it and run a first build.</p> <p>Continue: Download and install Maven →</p>","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"learning-paths/","level":1,"title":"Learning Paths","text":"","path":["Learning Paths"],"tags":[]},{"location":"usage/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]}]}